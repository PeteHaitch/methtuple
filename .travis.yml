language: python
python:
  - "2.7"

# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install:
    - pip install numpy
    - pip install cython
    - pip install pysam
    - python setup.py install
script:
    - comethylation --version
    - python setup.py test